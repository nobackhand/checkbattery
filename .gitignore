# Compiled executable (local build artifacts at repo root)
BatteryPill-*.exe
# ...but the released exe ships with the website so git-driven deploys serve it
!docs/BatteryPill-*.exe

# Runtime config (user-specific position data)
BatteryWidget.config.json

# Debug scripts
debug_dpi.ps1

# Claude plans
.claude/

# Playwright MCP session logs
.playwright-mcp/
